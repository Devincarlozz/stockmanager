# ── Credentials ───────────────────
# config.js is committed directly (anon key is public-facing, safe to commit)
# Real secrets (service role keys) should NEVER be committed

# ── OS junk ───────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Logs ──────────────────────────
*.log
